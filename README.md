# x-cli

A command-line interface for Twitter/X using cookie-based authentication.

## Install

```bash
uv sync
```

## Usage

```bash
x auth          # authenticate
x feed          # view timeline
x search "term" # search tweets
x post "text"   # post a tweet
```

All commands support `--json` for structured output (AI agent friendly).
